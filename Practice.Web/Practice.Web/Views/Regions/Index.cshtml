@model IEnumerable<Practice.Entities.Geographies.Region>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Country.CountryNameEnglish)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RegionNameEnglish)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Country.CountryNameEnglish)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RegionNameEnglish)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RegionCode }) |
            @Html.ActionLink("Details", "Details", new { id=item.RegionCode }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RegionCode })
        </td>
    </tr>
}

</table>
